package ai.montru.dust.stream;

import ai.montru.tokens.MontruDustTokens;
import ai.montru.tokens.MontruMindTokens;
import ai.montru.tokens.MontruUtilTokens;

public interface DustStreamTokens extends MontruUtilTokens, MontruMindTokens, MontruDustTokens {
}
